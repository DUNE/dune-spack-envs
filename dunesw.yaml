spack:

  include:
  - packages.yaml

  specs:
  - dune-sw %gcc12

  view: true
  concretizer:
    reuse:
      from:
      - type: local
      - type: external
    targets:
      granularity: generic
    unify: true
